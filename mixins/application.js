define(function (require) {
    "use strict";

    return function () {
        //this.mixin([DispatcherChanels]);

        this.setDefaults({
            
        });

        this.before('initialize', function () {
            //console.log('before init in application');
            //console.log(this);
        });

    };
});